## Summary

<!-- 简述本 PR 的目的与改动范围 -->

## Test plan

- [ ] 本地验证通过（如适用：`npm run build:web` / `npm run build:mp`）
- [ ] 涉及 `apps/client` 页面时，已确认 `*.config.ts` 仅导出静态配置（见 [CODE_REVIEW.md](../docs/CODE_REVIEW.md)）
- [ ] 涉及 `apps/client` 页面时，已运行 `npm run lint:taro-config`
